services:
  music-bot:
    build:
      context: "."
      dockerfile: "Dockerfile"
    image: viet-aus-it/music-bot
    restart: unless-stopped
    volumes:
      - ./music/config.txt:/src/config.txt
      - ./music/Playlists:/src/Playlists
